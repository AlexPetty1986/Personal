#include "RecordsFunctions.h"

using namespace std;

int main()
{

    const int division = 5;
    const int quarter = 5;
    double rowsTotal;
    double columnTotal;
    int rows;
    int columns;

    string div[division] = {"North", "South", "East", "West", "Quarter Total"};
    string qtr[quarter] = {"Quarter 1", "Quarter 2", "Quarter 3", "Quarter 4", "Division Total"};

    total = GetTotal(data);

    average = GetAverage(data);

    GetRowTotal();

    GetColumnTotal();

    highest = GetHighest(data, row, columns);

    lowest = GetLowest(data, rows, columns);

    return 0;
}
